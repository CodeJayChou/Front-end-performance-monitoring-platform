          ┌──────────────────────┐
          │     Application      │
          └─────────┬────────────┘
                    ↓
        ┌──────────────────────────┐
        │   SDK Public API Layer   │  (init / capture / setUser)
        └─────────┬────────────────┘
                  ↓
   ┌────────────────────────────────────┐
   │        Core Orchestration          │
   │  (event hub / context / scope)     │
   └───────┬───────────┬───────────────┘
           ↓           ↓
  ┌────────────┐ ┌──────────────┐
  │ Instrument │ │ Context Mgmt  │
  │  (hooks)   │ │ (scope store) │
  └─────┬──────┘ └──────┬───────┘
        ↓               ↓
   ┌────────────────────────────┐
   │     Event Processing       │
   │ normalize / filter / sample│
   └──────────┬─────────────────┘
              ↓
   ┌────────────────────────────┐
   │   Transport Layer          │
   │ queue / retry / batch / WS │
   └────────────────────────────┘

-    本质是做一个可拔插、可扩展、低侵入、可控采样、可稳定上报的事件采样系统 
-    前端监控SDK = 事件采集层 +  上下文增强层 + 上报可靠性层 
   
4个核心问题 
1.采什么 ？ 错误 、性能、行为、请求、自定义事件
2.怎么补全上下文 用户信息、路由、设备、日志栈
3.怎么稳定上报 重试机制 、队列、降级、批量
4.怎么不影响业务性能  异步、采样、限流 



