Monorepo 目录可以设计成：
frontend-monitor/

├── apps/
│
│   ├── dashboard/                 # 管理后台
│   │
│   ├── ingest-gateway/            # 数据接收网关
│   │
│   ├── consumer/                  # Kafka消费者
│   │
│   ├── alert-worker/              # 告警服务
│   │
│   ├── replay-worker/             # Replay处理服务
│   │
│   └── demo-app/                  # SDK测试应用
│
│
├── packages/
│
│   ├── sdk-core/                  # SDK核心（含本地缓存队列，原 storage-sdk 并入）
│   │
│   ├── sdk-error/                 # 错误监控
│   │
│   ├── sdk-performance/           # 性能监控
│   │
│   ├── sdk-network/               # XHR/Fetch监控
│   │
│   ├── sdk-behavior/              # 用户行为监控
│   │
│   ├── sdk-replay/                # Session Replay
│   │
│   ├── transport/                 # 上报层
│   │
│   ├── shared/                    # 公共工具（含日志能力，原 logger 并入）
│   │
│   └── types/                     # 公共类型
│
│
├── services/
│
│   ├── processor/                 # 数据处理（清洗/解析/入库）
│   │
│   ├── aggregator/                # 指标聚合
│   │
│   ├── query/                     # 查询服务
│   │
│   ├── alert-engine/              # 告警规则引擎
│   │
│   └── replay-engine/             # Replay解析
│
│
├── infrastructure/
│
│   ├── docker/
│   │
│   ├── k8s/
│   │
│   ├── clickhouse/
│   │
│   ├── kafka/
│   │
│   ├── redis/
│   │
│   ├── nginx/
│   │
│   └── prometheus/
│
│
├── database/
│
│   ├── schema/
│   │
│   ├── migrations/
│   │
│   └── seeds/
│
│
├── docs/
│
│   ├── architecture/
│   │
│   ├── sdk/
│   │
│   ├── replay/
│   │
│   ├── observability/
│   │
│   ├── deployment/
│   │
│   └── api/
│
│
├── scripts/
│
│   ├── release/
│   │
│   ├── build/
│   │
│   └── deploy/
│
│
├── .github/
│
│   └── workflows/
│
│
├── pnpm-workspace.yaml
├── turbo.json
├── tsconfig.base.json
├── eslint.config.js
└── package.json


-   核心数据流

Browser
    │
    ▼
sdk-core
    │
    ├── sdk-error
    ├── sdk-performance
    ├── sdk-network
    ├── sdk-behavior
    └── sdk-replay
    │
    ▼
transport
    │
    ▼
ingest-gateway
    │
    ▼
Kafka
    │
    ▼
consumer
    │
    ▼
ClickHouse
    │
    ├── processor
    ├── aggregator
    ├── query
    ├── alert-engine
    └── replay-engine
    │
    ▼
dashboard

Dashboard模块结构
dashboard/

src/

├── pages/
│
│   ├── overview/
│
│   ├── errors/
│
│   ├── performance/
│
│   ├── sessions/
│
│   ├── replay/
│
│   ├── alerts/
│
│   └── settings/
│
├── components/
│
├── services/
│
├── stores/
│
├── hooks/
│
└── utils/

-   SDK模块结构

sdk-core/

src/

├── core/
│
├── plugins/
│
├── lifecycle/
│
├── transport/
│
├── queue/
│
├── event-bus/
│
└── index.ts

插件化设计：
monitor.use(errorPlugin)

monitor.use(performancePlugin)

monitor.use(networkPlugin)

monitor.use(replayPlugin)